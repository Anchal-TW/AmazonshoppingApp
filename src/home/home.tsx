import {View, Text} from 'react-native';
import React from 'react';
import customStyles from '../style/style';

const Home = () => (
  <View style={customStyles.viewStyle}>
    <Text style={customStyles.textStyle}>Home</Text>
  </View>
);

export default Home;
